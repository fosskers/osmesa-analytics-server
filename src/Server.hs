module Main where

import Data.Aeson
import Types

---

-- display :: ToJSON a => a -> IO ()
-- display = TIO.putStrLn . toLazyText . encodePrettyToTextBuilder

main :: IO ()
main = pure ()
  -- display user
  -- generate (arbitrary @Leaderboard) >>= display
